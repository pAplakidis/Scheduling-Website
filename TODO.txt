backend (Pavlos):
- find a way to determine how days and hours available will be in restrictions table
- create an upload script for limitations file and finish up the limitations form
- create the algorithm that takes the limitations from the database and generates a schedule

frontend (Efthimis):
- finish index.html (simple intro and links to login and register) and add css to it
- make a page called schedule.html which calls the js file (this page can only be accessed if user is logged in, will be redirected there after registration or login)
- code schedule_gui.js
