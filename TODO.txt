backend:
- make an upload to the sql table
- create an upload script for limitations file and create a script to get them to the database

- for algorithm:
    - assign hours to the lectures
    - print two weeks of the schedule

- clean up the comments/code

frontend:
- finish index.html (simple intro and links to login and register) and add css to it
- make a page called schedule.html which calls the js file (this page can only be accessed if user is logged in, will be redirected there after registration or login)
- code schedule_gui.js

